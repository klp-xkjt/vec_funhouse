# vec_funhouse

一个Vector的小轮子

## 功能

- **单侧过滤** - 保留大于/小于阈值的元素
- **区间过滤** - 保留或排除区间内的元素（支持自定义边界）
- **多规则过滤** - 顺序应用多个过滤规则
- **闭包过滤** - 使用自定义闭包进行复杂过滤

## 快速开始

在 `Cargo.toml` 中添加：

```toml
[dependencies]
vec_funhouse = "0.1.0"